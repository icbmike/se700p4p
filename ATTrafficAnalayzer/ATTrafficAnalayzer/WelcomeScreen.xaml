<UserControl
    x:Class="ATTrafficAnalayzer.WelcomeScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="*" />
            <ColumnDefinition
                Width="*" />
        </Grid.ColumnDefinitions>
        <ListBox
            Grid.Column="0"
            Grid.Row="0"
            HorizontalAlignment="Stretch"
            Name="recentStandardReportsListBox"
            VerticalAlignment="Stretch"
            Margin="20" />
        <ListBox
            Grid.Column="1"
            Grid.Row="0"
            HorizontalAlignment="Stretch"
            Name="recentSpecialReportsListBox"
            VerticalAlignment="Stretch"
            Margin="20"/>
    </Grid>

</UserControl>
